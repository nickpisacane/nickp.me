import {createTheme} from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    // type: 'light',
    primary: {
      main: '#6200ea',
    },
    secondary: {
      main: '#f50057',
    },
  },
  typography: {
    fontFamily: 'Comfortaa',
  },
});
